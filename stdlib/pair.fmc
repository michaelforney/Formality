// Pair
// | A simple, non-dependent pair
// TODO: annotate type (follow Bits.fmc)
def Pair: {a b} [a, b]

// TODO: annotate type (follow Bits.fmc)
def Pair.fst: {pair}
  fst pair

// TODO: annotate type (follow Bits.fmc)
def Pair.snd: {pair}
  snd pair

// TODO: annotate type (follow Bits.fmc)
def Pair.prj: {pair func}
  get [val0, val1] = pair
  (func val0 val1)

// TODO: annotate type (follow Bits.fmc)
def Pair.swap: {pair}
  get [a, b] = pair
  [b, a]
